// 09
#include<stdio.h>
int main()
{
    int i,f,c,d;
    i = sizeof(int);
    f = sizeof(float);
    c = sizeof(char);
    d = sizeof(double);
    printf("size of int = %d\nsize of float = %d\nsize of char = %d\nsize of double = %d",i,f,c,d);
    return 0;
}
